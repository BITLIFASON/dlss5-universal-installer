{
  "NativeBridge": {
    "label_ru": "Native/Bridge",
    "label_en": "Native/Bridge",
    "requires": [
      "native_dlss"
    ],
    "notes": "Intercepts the game native DLSS path; usually adds minimal overhead."
  },
  "OptiScaler": {
    "label_ru": "OptiScaler",
    "label_en": "OptiScaler",
    "requires": [
      "dx11_or_dx12"
    ],
    "notes": "Replaces or intercepts the upscaler path; proxy DLL conflicts are possible."
  },
  "Feeder": {
    "label_ru": "ReShade + Feeder",
    "label_en": "ReShade + Feeder",
    "requires": [
      "dx11_or_dx12",
      "depth_buffer"
    ],
    "notes": "Builds a DLAA contract from the image, depth buffer, and motion vectors."
  }
}
